# Test Brief: Hello World API

Build a minimal FastAPI hello world service with:
- GET /health endpoint returning {"status": "ok"}
- GET /hello/{name} endpoint returning {"message": "Hello, {name}!"}
- Pydantic response models
- One test file using pytest

This is a smoke test to verify the Cressida pipeline works end-to-end.
Keep it minimal — 3-4 files max.
